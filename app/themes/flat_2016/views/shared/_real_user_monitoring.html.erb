<script>
var JETSCALE = (function() {
    var events = ("undefined" !== typeof(JETSCALE) && "undefined" !== typeof(JETSCALE.events) ? JETSCALE.events : []);

    function event(vars) {
        events.push(vars);
    }

    return {
        events: events,
        event: event
    }
})();
</script>
<script type="text/javascript" src="https://rum-static.jetscale.net/js-rum.min.js?id=savingsunited" async defer></script>
